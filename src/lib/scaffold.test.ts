import { describe, it, expect } from 'vitest'

// TEMPORARY. This test exists only to prove the test runner executes and reports
// correctly. Delete it in Milestone 1, when the first real compensation formula
// and its hand-calculated test replace it.
describe('test runner', () => {
  it('executes and reports a passing assertion', () => {
    expect(2 + 2).toBe(4)
  })
})
